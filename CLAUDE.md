# Portfolio

Personal developer portfolio site (Next.js + TypeScript + Tailwind + Framer Motion/GSAP), designed as an interactive introduction for recruiters/hiring managers rather than a static resume page. Deployed to Vercel from this GitHub repo.

## Agent skills

### Issue tracker

Local markdown under `.scratch/<feature>/`. See `docs/agents/issue-tracker.md`.

### Triage labels

Default vocabulary (needs-triage, needs-info, ready-for-agent, ready-for-human, wontfix). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout (`CONTEXT.md` + `docs/adr/` at repo root). See `docs/agents/domain.md`.
